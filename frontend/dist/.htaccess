DirectoryIndex disabled

RewriteEngine On

# Allow API health check and all /api routes through
RewriteCond %{REQUEST_URI} ^/api [NC]
RewriteRule ^(.*)$ http://localhost:%{ENV:PORT}/$1 [P,L]

# Proxy everything else to Node.js
RewriteRule ^(.*)$ http://localhost:%{ENV:PORT}/$1 [P,L]
